/**
1, The $CFILE variale was compile with gcc in bash script and it was instructed to stop at preprocessor then print the output in c. 
i first create a main.c file with a message "hello world" in it. then i export CFILE=main.c then i make it executable files, with chmod u+x then i run ./file-name
