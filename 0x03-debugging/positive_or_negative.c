#include <stdio.h>
#include "main.h"

/*
* This is a function that test the function positive_or_negative() with 0.
*/


void positive_or_negative(int i)
{

	if (i >= 0 ) 
	{
		printf("%d is zero\n",i);
	}
	else 
		{
			printf("%d is not zero\n",i);
		}
}
